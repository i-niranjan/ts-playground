const superHero: string[] = [];
const table: number[] = [];
superHero.push("Spiderman");

table.push(2);

const tableArray: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};
const allUsers: User[] = [];

allUsers.push({ name: "niranjan", isActive: true });

const arrayOf: number[][] = [
  [1, 2, 3],
  [1, 2, 3],
];
